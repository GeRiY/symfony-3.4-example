{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
<div class="container">
    <div style="text-align: center;"><h1 style="text-align: center;margin-top:5px;margin-bottom:40px;">Profil szerkeztés</h1></div>
    <div class="row">
        <div class="col-lg-2">
            {{ form_label(form.username) }}:<br><br>
            {{ form_label(form.email) }}:<br><br>
            {{ form_label(form.current_password) }}:
        </div>
        <div class="col-lg-2">
            {{ form_widget(form.username) }}<br><br>
            {{ form_widget(form.email) }}<br><br>
            {{ form_widget(form.current_password) }}
        </div>
        <div class="col-lg-12">
            <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
        </div>
    </div>
</div>
{{ form_end(form) }}
